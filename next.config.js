/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['i.redd.it', 'cryptologos.cc', 'lh3.googleusercontent.com']
  }
}

module.exports = nextConfig
